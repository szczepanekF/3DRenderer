#include "view/MainGameView.h"

// TODO : en passant, transformation, maybe(checkmate detection and showing possible moves)
// and castling move TESTS :/

int main(void) {
    MainGameView view;
    view.run();
    return 0;
}

